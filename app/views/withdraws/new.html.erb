<div class="row justify-content-center vh-100">
    <div class="col-8 align-self-center">
        <div class="auth-page">
            <div class="card auth-card shadow-lg">
                <div class="card-body">
                    <div class="px-3">
                      <% @welcomes.take(1).each do |w| %>
                      <div class="auth-logo-box">
                        <%= image_tag w.logoimg_url.to_s, class: "img-responsive inline-block" %>
                      </div>
                      <% end %>
                      <br>
                      <br>
                      <h3 class="inline-block pull-right no-mrg-vertical pdd-top-15">Withdrawals</h3>


<%= render 'form', withdraw: @withdraw %>

<%= link_to 'Back', withdraws_path %>
</div>
</div>
</div>
</div>
</div>
</div>
