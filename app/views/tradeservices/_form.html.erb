
<%= simple_form_for(@tradeservice) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :slug %>
    <%= f.input :icon %>
    <%= f.input :sub_title %>
    <%= f.input :body %>
    <%= f.input :tserviceimg %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
